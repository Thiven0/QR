import HeaderImages from "./HeaderImages";
import LoginForm from "./LoginForm";

const Login = () => {
  return (
    <div className="relative flex items-center justify-center min-h-screen bg-gradient-to-b from-[#FFFFFF] to-[#7DC189] via-[#FFFFFF] 50%">
      <HeaderImages />
      <LoginForm />
    </div>
  );
};

export default Login;
